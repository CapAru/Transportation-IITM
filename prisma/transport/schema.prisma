generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/.prisma/client-transport"
}

datasource db {
  provider = "postgresql"
  url      = env("TRANSPORT_DATABASE_URL")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model device101_102_merged {
  id        String   @id @map("_id")
  mac       String?
  rssi      Decimal?  @db.Decimal(6, 2)
  timestamp DateTime? @db.Timestamp(6)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model device103_104_merged {
  id        String   @id @map("_id")
  mac       String?
  rssi      Decimal?  @db.Decimal(6, 2)
  timestamp DateTime? @db.Timestamp(6)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model device105_106_merged {
  id        String   @id @map("_id")
  mac       String?
  rssi      Decimal?  @db.Decimal(6, 2)
  timestamp DateTime? @db.Timestamp(6)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model device107_108_merged {
  id        String   @id @map("_id")
  mac       String?
  rssi      Decimal?  @db.Decimal(6, 2)
  timestamp DateTime? @db.Timestamp(6)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model device109_110_merged {
  id        String   @id @map("_id")
  mac       String?
  rssi      Decimal?  @db.Decimal(6, 2)
  timestamp DateTime? @db.Timestamp(6)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model device111_112_merged {
  id        String   @id @map("_id")
  mac       String?
  rssi      Decimal?  @db.Decimal(6, 2)
  timestamp DateTime? @db.Timestamp(6)
}

model sample_gps {
  id              Int      @id
  time            String?  @db.VarChar(10)
  date            String?  @db.VarChar(10)
  latitude        Decimal? @db.Decimal(9, 6)
  longitude       Decimal? @db.Decimal(9, 6)
  speed           Decimal? @db.Decimal(5, 2)
  signal_strength Int?
}
